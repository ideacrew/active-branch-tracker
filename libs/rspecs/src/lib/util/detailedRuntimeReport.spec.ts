import { rspecExamples } from '../mocks';

describe('The detailed runtime report', () => {
  // it('should create a detailed runtime report from a list of examples', () => {
  //   expect(detailedRuntimeReport(rspecExamples)).toEqual([{ file: 'xyz', }]
  // });
});
