import { EnvironmentsEntity } from '../store/environments.models';

// export function createEnvironmentList(
//   environments: EnvironmentsEntity[],
// ): any[] {
//   const organizations: string[] = environments.reduce((orgs, environment) => {
//     const org = environment.latestDeployment.org;

//     return [...orgs, org];
//   }, []);

//   const uniqueOrgs = new Set(organizations);
// }
